1.Мова JavaScript існує як підвид мови Java?
·      Ні;

2. JavaScript це?
·      Динамічно типізована мова програмування;

3.Всередині якого HTML тега ми пишемо або підключаємо JavaScript?
·   	<script>;

4.Як можна оголосити змінну в JS?
·      let, const, var
 
5.Змінні name і Name (з великої літери) - це одна і та ж або різні?
·      Різні

6.Якого типу даних в Javascript не існує?
·      int
 
7.Яка вбудована функція в JavaScript виводить модальне вікно з повідомленням «Logos».
·      alert(“Logos”);
 
8.Який оператор з перерахованих виконує не тільки математичні операції?
·      +;
 
9.Які конструкції для умов існують в JavaScript?
·      if, else if та else;
 
10.Які конструкції для циклів існують в JavaScript?
·      for, while та do…while;
 
11.Який коректний варіанти оголошення функції в JavaScript?
·      function f(){};
 
12. Яка арифметична операція веде до помилки в JavaScript?
·  	Жодна з вище написаних
 
13. Що з перерахованого не є коректним JavaScript виразом, що описує цикл?
·  	let i=0;while(i<5)do{i++;}
 
14. Виберіть усі виклики parseInt які повернуть число!
·       parseInt(“1px”);
·       parseInt(“-1.2”);
·       parseInt(“5 minutes”);
 
15. Чому буде дорівнювати значення змінної після виконання даного коду?
let a = 1;
let c = a+++1;
console.log(c);
 
·       2
 
16. Що виведеться на при виконанні наступного коду?       	
let a = false;
if (a == "0") alert("==");
if (a === "0") alert("===");
 
·       ==;
 
17. Що виведеться в console.log при виконанні ф-ї list
function list(a, b, ...arg) {
    console.log(arg);
}
list(1, 2, 3, 4, 5);
 
·       [3,4,5]
 
18. Що робить дана частина коду? break extra

·       Видасть помилку;
 
19. Скільки параметрів (аргументів) можна передати функції?

·       Скільки завгодно;

20. Чи є різниця між цими двома функціями?
function f(a, b) {
    return a + b;
}
let f = function (a, b) {
    return a + b;
}
 
·       Так є, тільки коли викликаємо функцію перед її створенням;
